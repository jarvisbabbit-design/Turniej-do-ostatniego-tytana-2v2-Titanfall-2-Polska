# Turniej Do Ostatniego Tytana 2v2

Pliki do wrzucenia na GitHub Pages:

- `index.html`
- `style.css`
- `app.js`
- `firebase-config.js`

Firebase Realtime Database zapisuje dane w gałęzi `tournament`.
Domyślne hasło admina znajduje się w `app.js` jako `ADMIN_PASSWORD`.
